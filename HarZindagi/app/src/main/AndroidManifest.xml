<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ipal.itu.harzindagi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data android:name="AA_DB_NAME" android:value="HZDb.db" />
        <meta-data android:name="AA_DB_VERSION" android:value="1" />
<!--
        //TODO:
        There seems to be an error in these lines.
        I am not able to compile the source code at my end because of these.
        Please look into this.

        <meta-data
            android:name="AA_MODELS"
            android:value="com.ipal.itu.harzindagi.Entity.ChildInfo" />
        <meta-data
            android:name="AB_MODELS"
            android:value="com.ipal.itu.harzindagi.Entity.Transaction" />
            -->
    </application>

</manifest>
